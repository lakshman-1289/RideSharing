# Server Configuration
server.port=8082

# Application Name
spring.application.name=ride-service

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/ride_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=reddi2273
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
# 'update' mode: Updates schema if needed, preserves existing data
# 'create' mode: Drops and recreates tables (DELETES ALL DATA on restart)
# 'validate' mode: Only validates schema, no changes (safest for production)
# 'none' mode: No schema management (manual control)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
# Additional Hibernate properties to handle schema issues
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
# Force Hibernate to validate and update schema on startup
spring.jpa.properties.hibernate.hbm2ddl.auto=update
# Ensure entities are scanned from the correct package
spring.jpa.properties.hibernate.hbm2ddl.import_files=

# =========================
# Maps API Configuration (OpenRouteService)
# =========================
# OpenRouteService API Key (free, open-source routing service)
# Get your API key from: https://openrouteservice.org/dev/#/signup
google.maps.api.key=eyJvcmciOiI1YjNjZTM1OTc4NTExMTAwMDFjZjYyNDgiLCJpZCI6IjlmOGQyMjllNjYwODQ0ZWI4ZTg0YjM2ZGUwNDY4YWQzIiwiaCI6Im11cm11cjY0In0=
# OpenRouteService Directions API URL (using driving-car profile)
google.maps.distance-matrix.url=https://api.openrouteservice.org/v2/directions/driving-car

# =========================
# Fare Calculation Settings
# =========================
# Base fare applied to every ride/segment
fare.base-fare=50.0
# Rate per kilometer
fare.rate-per-km=10.0
# Currency code for fare values
fare.currency=INR

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
# Use hostname instead of IP address for local development
eureka.instance.prefer-ip-address=false
eureka.instance.hostname=localhost
eureka.instance.instance-id=${spring.application.name}:${server.port}

# Feign Client Configuration
feign.client.config.default.connect-timeout=5000
feign.client.config.default.read-timeout=10000

# Logging Configuration
logging.level.com.ridesharing.rideservice=INFO
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

# Application Info
info.app.name=Smart Ride Sharing System - Ride Service
info.app.description=Ride Posting, Searching, and Booking Service
info.app.version=1.0.0

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=campusworks2273@gmail.com
spring.mail.password=nlbdkysxhffrjwnt
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8

# Route Matching Configuration
# Maximum distance (in meters) a passenger point can be from driver's route polyline
# Default: 75000m (75km) - increased for better partial matching (Indian cities can be 20-50km apart)
route.matching.max-distance-meters=75000
# Maximum distance (in km) for coordinate-based fallback matching
route.matching.max-distance-km=50.0
# Number of intermediate waypoints to generate for synthetic polyline (when geometry missing)
# Increased to 15 for better accuracy on long routes (Hyderabad-Vijayawada is ~270km)
route.matching.synthetic-waypoints=15

# WebSocket Configuration
# Allow CORS for WebSocket connections from frontend
spring.websocket.allowed-origins=http://localhost:5173,http://localhost:3000

# Ride Reminder Configuration
# Hours before ride to send reminder notifications (default: 2 hours)
ride.reminder.hours-before=2
# Enable/disable ride reminders (default: true)
ride.reminder.enabled=true

# Configure static resource handling to only serve from /static/** and not from /api/**
spring.web.resources.static-locations=classpath:/static/
spring.mvc.static-path-pattern=/static/**

